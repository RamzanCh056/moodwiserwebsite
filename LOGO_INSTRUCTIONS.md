# How to Add Your Moodwiser Logo

## 📍 Where to Place Your Logo

1. **Place your logo file** in the `public` folder:
   - File name: `moodwiser-logo.png` (or `.jpg`, `.svg`)
   - Recommended size: 512x512 pixels (square)
   - Format: PNG (transparent background recommended) or SVG

2. **Update the file name** in `app/page.tsx` if you use a different name:
   - Search for: `/moodwiser-logo.png`
   - Replace with your file name

## 🎨 Logo Requirements

- **Format**: PNG, JPG, or SVG
- **Size**: 512x512px (or larger, will be scaled down)
- **Background**: Transparent PNG recommended for best results
- **File location**: `/public/moodwiser-logo.png`

## 📱 Favicon (Browser Tab Icon)

To update the favicon (the small icon in browser tabs):

1. Replace `app/favicon.ico` with your own favicon
2. Or create these files in the `app` folder:
   - `icon.png` (512x512px)
   - `apple-icon.png` (180x180px for iOS)

## ✅ Current Logo Locations

Your logo will appear in:
- ✅ Header (top navigation)
- ✅ Footer (company bio section)

After adding your logo file, refresh your browser to see the changes!

